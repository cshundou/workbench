### 阶段 1：生成完整项目脚手架

**目标**：生成前后端分离的项目骨架，所有依赖和配置文件都正确

**执行方式**：打开 Composer，粘贴以下指令，点击 "Run"

plaintext

```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第9.1节的要求，生成完整的项目脚手架：

## 项目结构
创建以下目录结构：
ai-workbench/
├── frontend/          # Vue 3 + Vite + TypeScript 前端
├── backend/           # FastAPI 后端
├── docs/              # 文档目录
├── docker-compose.yml
├── .env.example
└── README.md

## 前端要求（严格遵循文档第2.1节）
- 技术栈：Vue 3 + Vite 5 + TypeScript 5 + Pinia 2 + Vue Router 4 + Element Plus 2
- 配置：ESLint 8 + Prettier 3 + Husky 9 + UnoCSS
- 生成：package.json、vite.config.ts、tsconfig.json、.eslintrc.cjs、.prettierrc
- 生成：基础布局组件（侧边栏、顶部导航、主内容区）
- 生成：登录页和首页骨架
- 生成：Pinia Store 目录结构和基础文件
- 生成：Axios 请求封装和 SSE 工具类骨架

## 后端要求（严格遵循文档第2.2节）
- 技术栈：Python 3.11 + FastAPI 0.110 + SQLAlchemy 2.0
- 生成：requirements.txt（包含所有依赖的精确版本号）
- 生成：项目目录结构（app/api, app/models, app/services, app/core）
- 生成：数据库连接配置和ORM基础类
- 生成：JWT认证中间件和异常处理中间件
- 生成：CORS配置和日志配置
- 生成：FastAPI启动文件 main.py

## 部署配置
- 生成：前端 Dockerfile 和 后端 Dockerfile
- 生成：docker-compose.yml（包含PostgreSQL 16、Redis 7、前后端服务）
- 生成：.env.example 文件，包含所有必要的环境变量

## 输出要求
1. 所有文件都要完整可运行，不要有占位符
2. 依赖版本必须与文档完全一致
3. 代码中添加必要的注释
4. 在README.md中添加本地启动说明
```

**验证标准**（完成后必须检查）：

1. 执行 `docker compose up --build` 能成功启动所有服务
2. 访问 `http://localhost:5173` 能看到前端页面
3. 访问 `http://localhost:8000/docs` 能看到 FastAPI 接口文档
4. 执行 `git add . && git commit -m "feat: 初始化项目脚手架"` 提交代码

------

### 阶段 2：用户体系与数据库设计

**目标**：实现完整的 RBAC 权限系统和数据库表结构

**执行方式**：打开 Composer，粘贴以下指令

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第4章和第6.1节的要求，实现用户体系与数据库设计：

## 数据库表
1. 严格按照文档第4.2节的SQL语句，在 backend/app/models/ 下生成所有SQLAlchemy模型
2. 生成数据库迁移文件（使用alembic）
3. 生成初始化数据脚本（创建默认租户、默认角色和admin用户）

## 后端接口
实现文档第8.2节的所有用户认证接口：
- POST /api/v1/auth/login
- GET /api/v1/auth/me
- POST /api/v1/auth/logout
- 用户CRUD接口
- 角色CRUD接口

## 前端实现
1. 实现登录页面和退出功能
2. 实现动态权限路由和路由守卫（文档第7.4节）
3. 实现用户管理页面和角色管理页面
4. 实现Pinia的userStore（文档第7.2节）

## 核心要求
1. 所有表结构、字段名、索引必须与文档完全一致
2. 密码使用bcrypt加密存储
3. JWT令牌有效期24小时
4. 所有接口都要有权限校验
5. 前端未登录自动跳转到登录页
```

**验证标准**：

1. 执行 `alembic upgrade head` 能成功创建所有数据库表
2. 能使用默认账号 `admin / admin123` 登录
3. 能创建新用户和新角色
4. 不同权限的用户看到不同的菜单
5. 提交 Git：`git commit -m "feat: 实现用户体系与权限系统"`

------

### 阶段 3：增强 RAG 系统实现

**目标**：实现文档第 5.1 节的 7 层增强 RAG 系统，这是项目核心

**执行方式**：分两步执行，先后端再前端

#### 第一步：后端 RAG 引擎

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第5.1节的要求，实现完整的增强版RAG系统后端：

## 实现文档第5.1.1节的7层架构
1. 文档接入层：实现 DocumentLoader 类，支持PDF/MD/TXT/Excel/Word格式
2. 智能分块层：实现 IntelligentChunker 类，混合分块+语义分块+标题锚定
3. 元数据增强层：为每个分块添加完整的元数据
4. 双路检索层：实现 HybridRetriever 类，向量检索+BM25检索
5. 重排序层：实现 Reranker 类，使用Cohere Rerank模型
6. 上下文拼接层：实现 ContextBuilder 类，父子块检索
7. 引用溯源层：实现 AnswerGenerator 类，自动标注引用来源

## 后端接口
实现文档第8.3节的所有知识库接口：
- 知识库CRUD接口
- 文档上传、下载、删除接口
- 文档解析进度查询接口
- 知识库检索接口
- 流式问答接口（SSE）

## 核心要求
1. 所有代码严格按照文档中的示例实现
2. 文档解析使用异步任务，后台执行
3. 支持增量更新向量库，不需要全量重建
4. 支持按部门、文档类型、时间过滤检索结果
5. 回答必须包含引用来源信息
```

#### 第二步：前端 RAG 页面

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第6.2节和第7章的要求，实现知识库前端页面：

1. 知识库列表页面：展示所有有权限的知识库，支持创建、编辑、删除
2. 知识库详情页面：展示文档列表，支持上传文档、预览文档、删除文档
3. 问答页面：实现流式输出聊天界面（使用文档第7.1节的SSE封装）
4. 引用溯源组件：点击引用可以跳转到对应的文档片段
5. 实现Pinia的ragStore（文档第7.2节）

## 核心要求
1. 文档上传支持拖拽和批量上传
2. 显示文档解析进度条
3. 大模型回答逐字流式输出
4. 支持Markdown、代码块、表格的自动渲染
5. 支持中断当前回答
```

**验证标准**：

1. 能上传 PDF 文档并成功解析
2. 能对文档进行提问，得到准确的回答
3. 回答中包含引用来源，点击可以查看原文
4. 提交 Git：`git commit -m "feat: 实现增强RAG系统"`

------

### 阶段 4：单 Agent 智能体系统

**目标**：实现可配置的工具型 Agent，支持多种外部工具调用

**执行方式**：

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第5.2节的要求，实现单Agent智能体系统：

## 后端实现
1. 实现工具基类 BaseTool 和工具注册机制
2. 实现以下内置工具（文档第5.2.1节）：
   - 知识库检索工具（对接已实现的RAG系统）
   - Tavily联网搜索工具
   - Python代码执行工具
   - 自然语言转SQL工具
3. 实现 AgentService 类，基于LangChain的OpenAI Tools Agent
4. 实现异常兜底机制：工具调用失败重试、Token超限保护、超时处理
5. 实现Agent对话接口（支持流式输出和思考过程推送）

## 前端实现
1. 智能体列表页面：展示所有智能体，支持创建、编辑、删除、复制
2. 智能体配置页面：支持修改系统提示词、选择可用工具、调整模型参数
3. 智能体对话页面：实现思考过程可视化，展示每一步工具调用
4. 工具调用记录组件：展开/折叠查看工具的入参和返回结果
5. 实现Pinia的agentStore

## 核心要求
1. Agent能自主判断是否需要调用工具
2. 前端实时显示Agent的思考状态："正在分析问题"、"调用知识库检索"等
3. 支持中断当前Agent执行
4. 保存完整的对话历史，包括工具调用过程
```

**验证标准**：

1. 能创建一个新的智能体，配置系统提示词和工具
2. 能与智能体对话，智能体能正确调用工具解决问题
3. 前端能看到完整的工具调用过程和结果
4. 提交 Git：`git commit -m "feat: 实现单Agent智能体系统"`

------

### 阶段 5：LangGraph 多智能体工作流

**目标**：实现项目最高技术亮点 —— 多智能体协同工作流

**执行方式**：

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第5.3节的要求，实现LangGraph多智能体工作流系统：

## 后端实现
1. 实现 AgentState 类型定义（文档第5.3.2节）
2. 实现5个核心智能体节点：
   - 调度中心Agent：任务拆解和分配
   - 知识库Agent：调用RAG系统
   - 搜索Agent：联网搜索
   - 执行Agent：代码和SQL执行
   - 审核Agent：结果汇总和审核
3. 实现标准工作流拓扑：串行、并行、条件分支
4. 实现人工介入节点功能
5. 使用RedisSaver实现工作流状态持久化
6. 实现工作流CRUD接口和执行接口
7. 实现WebSocket接口，实时推送工作流执行状态

## 前端实现
1. 工作流列表页面：展示所有工作流模板
2. 工作流执行页面：使用vue-flow实现工作流拓扑图可视化
3. 节点状态显示：灰色(等待)、蓝色(执行中)、绿色(完成)、红色(失败)
4. 执行日志面板：按时间线展示所有节点的执行日志
5. 人工介入交互：支持确认或拒绝继续执行
6. 实现Pinia的graphStore

## 核心要求
1. 工作流支持中断和恢复，服务重启不丢失状态
2. 前端拓扑图支持放大、缩小、拖拽
3. 点击节点可以查看详细的输入输出
4. 支持同时运行多个工作流实例
```

**验证标准**：

1. 能执行一个完整的多智能体工作流
2. 前端能实时看到工作流的执行进度和节点状态
3. 能处理包含人工介入的工作流
4. 提交 Git：`git commit -m "feat: 实现LangGraph多智能体工作流"`

------

### 阶段 6：系统监控与部署

**目标**：完善系统功能，实现容器化部署

**执行方式**：

plaintext









```
@docs/DEVELOPMENT_DOCUMENT.md
基于文档第6.6节和第11章的要求，实现系统监控与部署：

## 系统监控
1. 实现Token消耗统计接口，按用户、模型、时间统计
2. 实现接口调用量和响应时间统计
3. 实现错误日志收集和查询接口
4. 前端实现监控仪表盘，使用ECharts展示统计图表

## 部署优化
1. 完善docker-compose.yml，添加Nginx反向代理
2. 优化Dockerfile，减小镜像体积
3. 添加接口限流和安全防护
4. 完善.env.example文件，添加所有必要的配置说明
5. 编写完整的部署文档README.md

## 最终完善
1. 修复所有已知的bug
2. 优化前端性能：添加虚拟滚动、组件懒加载
3. 完善异常处理和用户提示
4. 统一代码风格和命名规范
```

**验证标准**：

1. 执行 `docker compose up -d` 能一键启动所有服务
2. 监控仪表盘能正确显示统计数据
3. 所有功能都能正常使用
4. 提交 Git：`git commit -m "feat: 完成系统监控与部署优化"

------

## 📋 完整执行时间表

表格







|       阶段       |   预计时间    |            核心产出            |
| :--------------: | :-----------: | :----------------------------: |
|     前置准备     |    10 分钟    |      项目初始化、规则配置      |
|    项目脚手架    |    15 分钟    |    前后端骨架、Docker 配置     |
| 用户体系与数据库 |    30 分钟    |    RBAC 权限系统、登录注册     |
|  增强 RAG 系统   |    1 小时     |   7 层 RAG 引擎、知识库问答    |
|  单 Agent 系统   |    45 分钟    |  工具型 Agent、思考过程可视化  |
| LangGraph 工作流 |    1 小时     |    多智能体协同、拓扑可视化    |
|    监控与部署    |    30 分钟    |      监控仪表盘、一键部署      |
|     **总计**     | **约 4 小时** | **完整可运行的企业级 AI 应用** |

------

## ✅ 最终交付物

完成所有步骤后，你将得到：

1. 一个完整可运行的企业智能协作工作台
2. 严格遵循开发文档的代码库
3. 一键部署的 Docker 配置
4. 完整的接口文档和部署说明
5. 一份极具竞争力的项目经历

需要我为你生成**每个阶段的验证检查清单**，确保每一步都符合文档要求吗？