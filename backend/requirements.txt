# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.1

# Database（asyncpg>=0.30 提供 Python 3.13+ 预编译 wheel；Docker 内 Python 3.11 同样兼容）
sqlalchemy>=2.0.36  # 2.0.29 在 Python 3.13 下不兼容，Docker Python 3.11 同样可用
greenlet>=3.0.0  # SQLAlchemy 异步操作依赖
asyncpg>=0.30.0
alembic==1.13.1

# Configuration & validation
pydantic-settings==2.2.1
email-validator==2.1.1

# Authentication & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1  # passlib 与 bcrypt 5.x 不兼容
python-multipart==0.0.9

# Cache & HTTP client
redis==5.0.3
arq==0.26.3
httpx==0.27.0

# AI / LangChain ecosystem
langchain==0.1.20
langchain-community==0.0.38
langchain-openai==0.1.7
langgraph==0.0.26
langchain-experimental==0.0.58

# Vector store & document processing
numpy<2  # langchain 0.1.x 要求 numpy 1.x
rank-bm25==0.2.2  # BM25 混合检索依赖
chromadb==0.4.24
unstructured==0.11.8
networkx>=3.0  # unstructured 传递依赖
pypdf2==3.0.1
python-docx==1.1.0
pandas==2.2.1
openpyxl==3.1.2
python-pptx==1.0.2
beautifulsoup4==4.12.3

# External tools
cohere>=5.11.0  # >=5.11 兼容 tokenizers 0.20+（Python 3.13 wheel）
tokenizers>=0.20.0
tavily-python==0.3.3

# Observability
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
